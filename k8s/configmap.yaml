apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudops-config
  namespace: cloudops

data:
  NODE_ENV: "production"
  PORT: "5000"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "cloudops"
